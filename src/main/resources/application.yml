spring:
  application:
    name: Botanify
  profiles:
    active: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/botanify
    username: admin
    password: admin
